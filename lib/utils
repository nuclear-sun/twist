#! /bin/bash

source ../conf/env

function log(){
	datetime=$(date +'%Y-%m-%d %H:%M')
	echo "[${datetime}] $0 $@"
}

function input(){
	echo "$@" >> ${INPUT_CHANEL}
}

function record_pid(){
	echo "$?" >> ${RUNNING_PID_FILE}
}
